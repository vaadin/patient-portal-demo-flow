<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.vaadin.flow.demo</groupId>
    <artifactId>patient-portal-flow</artifactId>
    <name>patient-portal-flow</name>
    <version>3.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.source>${maven.compiler.release}</maven.compiler.source>
        <maven.compiler.target>${maven.compiler.release}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <vaadin.version>25.1-SNAPSHOT</vaadin.version>
        <flow.version>${vaadin.version}</flow.version>

        <!-- Local server configuration (used in default profile) -->
        <app.port>8081</app.port>
        <management.port>8082</management.port>
        <proxy.port>6000</proxy.port>

        <!-- K6 test configuration -->
        <k6.vus>100</k6.vus>
        <k6.duration>30s</k6.duration>
        <k6.testDir>${project.build.directory}/k6/tests</k6.testDir>

        <!-- Combined scenario weights (percentage of VUs for each scenario) -->
        <k6.combineScenarios>true</k6.combineScenarios>
        <k6.scenarioWeights>patientEditor:30,navigation:70</k6.scenarioWeights>
        <!-- <k6.scenarioWeights>journalEditor:30,navigation:30</k6.scenarioWeights> -->
        <!-- <k6.scenarioWeights>patientEditor:40,journalEditor:30,navigation:20</k6.scenarioWeights> -->
        <!-- <k6.scenarioWeights>patientEditor:70,journalEditor:30</k6.scenarioWeights> -->
<!--        <k6.scenarioWeights>patientEditor:100</k6.scenarioWeights>-->

        <!-- Enable Vaadin-specific metrics collection (requires VaadinActuator in demo app) -->
        <k6.collectVaadinMetrics>true</k6.collectVaadinMetrics>

        <!-- Skip flags for flexible execution -->
        <k6.skipRecord>false</k6.skipRecord>
        <k6.skipRun>false</k6.skipRun>
    </properties>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.4</version>
    </parent>

    <pluginRepositories>
        <pluginRepository>
            <id>Vaadin Prereleases</id>
            <url>https://maven.vaadin.com/vaadin-prereleases</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
        <pluginRepository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>localrepository</id>
            <url>file://${basedir}/localrepo</url>
            <snapshots>
                <checksumPolicy>ignore</checksumPolicy>
            </snapshots>
        </repository>
        <repository>
            <id>Vaadin Prereleases</id>
            <url>https://maven.vaadin.com/vaadin-prereleases</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <!-- Repository used by many Vaadin add-ons -->
        <repository>
            <id>Vaadin Directory</id>
            <url>https://maven.vaadin.com/vaadin-addons</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <version>${vaadin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>com.vaadin</groupId>
            <!-- Replace artifactId with vaadin-core to use only free components -->
            <artifactId>vaadin</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
            <exclusions>
                <!-- Hilla is not used -->
                <exclusion>
                    <groupId>com.vaadin</groupId>
                    <artifactId>hilla</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>flow-polymer-template</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.vaadin.demo</groupId>
            <artifactId>patient-portal-backend</artifactId>
            <version>3.0.0-SNAPSHOT</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.tomcat</groupId>
                    <artifactId>tomcat-juli</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>

        <!-- Spring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- /Spring -->

        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-testbench</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hsqldb</groupId>
            <artifactId>hsqldb</artifactId>
            <version>2.4.0</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>flow-test-util</artifactId>
            <version>${flow.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.github.stephankoelle</groupId>
            <artifactId>jamm</artifactId>
            <version>0.4.1</version>
        </dependency>
        <dependency>
        <groupId>com.vaadin</groupId>
            <artifactId>testbench-loadtest-support</artifactId>
            <version>10.1-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>loadtest-error-handler</artifactId>
            <version>10.1-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>spring-boot:run</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <!--
                Take care of synchronizing java dependencies and imports in
                package.json and main.js files.
                It also creates webpack.config.js if not exists yet.
            -->
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-maven-plugin</artifactId>
                <version>${vaadin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-frontend</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <!-- Production mode is activated using -Pproduction -->

            <id>production</id>
            <dependencies>
                <dependency>
                    <groupId>com.vaadin</groupId>
                    <artifactId>flow-server-production-mode</artifactId>
                </dependency>
            </dependencies>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <jvmArguments>-Dvaadin.productionMode</jvmArguments>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>com.vaadin</groupId>
                        <artifactId>vaadin-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>build-frontend</goal>
                                </goals>
                                <phase>compile</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>all-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>getClasspathFilenames</id>
                                <goals>
                                    <goal>properties</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <maxAttempts>100</maxAttempts>
                            <wait>2500</wait>
                            <managementPort>${management.port}</managementPort>
                            <arguments>
                                <argument>--server.port=${app.port}</argument>
                            </arguments>
                            <additionalClasspathElements>
                                <additionalClasspathElement>${project.build.testOutputDirectory}</additionalClasspathElement>
                            </additionalClasspathElements>
                            <agents>${io.github.stephankoelle:jamm:jar}</agents>
                            <jvmArguments>
                                -XX:+UseParallelGC
                            </jvmArguments>
                        </configuration>
                        <executions>
                            <execution>
                                <id>start-spring-boot</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>stop-spring-boot</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Runs the integration tests (*IT) after the server is started -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <trimStackTrace>false</trimStackTrace>
                            <enableAssertions>true</enableAssertions>
                            <systemPropertyVariables>
                                <app.port>${app.port}</app.port>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>

                </plugins>
            </build>
        </profile>

        <profile>
            <id>local</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <!-- Orchestrate local server lifecycle -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.1</version>
                        <executions>
                            <!-- Start the demo app -->
                            <execution>
                                <id>start-demo-app</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>java</executable>
                                    <arguments>
                                        <argument>-jar</argument>
                                        <argument>target/${project.build.finalName}.jar</argument>
                                        <argument>--server.port=${app.port}</argument>
                                        <argument>--management.server.port=${management.port}</argument>
                                    </arguments>
                                    <async>true</async>
                                    <asyncDestroyOnShutdown>true</asyncDestroyOnShutdown>
                                </configuration>
                            </execution>

                            <!-- Stop demo app -->
                            <execution>
                                <id>stop-demo-app</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>bash</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>
                                            echo "Stopping demo app..."
                                            pkill -f "${project.build.finalName}.jar.*--server.port=${app.port}" || true
                                        </argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Wait for app to be fully ready before running tests -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>wait-for-app</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <echo>Waiting for health endpoint on port ${management.port}...</echo>
                                        <waitfor maxwait="2" maxwaitunit="minute" checkevery="2" checkeveryunit="second">
                                            <http url="http://localhost:${management.port}/actuator/health" errorsBeginAt="500"/>
                                        </waitfor>
                                        <echo>Health endpoint ready. Waiting for app on port ${app.port}...</echo>
                                        <waitfor maxwait="2" maxwaitunit="minute" checkevery="2" checkeveryunit="second">
                                            <http url="http://localhost:${app.port}/" errorsBeginAt="500"/>
                                        </waitfor>
                                        <echo>App is ready on port ${app.port}!</echo>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- K6 TestBench Converter Plugin -->
                    <plugin>
                        <groupId>com.vaadin</groupId>
                        <artifactId>testbench-converter-plugin</artifactId>
                        <version>10.1-SNAPSHOT</version>
                        <executions>
                            <!-- Record TestBench scenarios -->
                            <execution>
                                <id>record-scenarios</id>
                                <phase>integration-test</phase>
                                <goals>
                                    <goal>record</goal>
                                </goals>
                                <configuration>
                                    <skip>${k6.skipRecord}</skip>
                                    <testClasses>
                                        <!-- <testClass>JournalEditorIT</testClass> -->
                                        <testClass>PatientEditorIT</testClass>
                                        <testClass>NavigationIT</testClass>
                                    </testClasses>
                                    <proxyPort>${proxy.port}</proxyPort>
                                    <appPort>${app.port}</appPort>
                                    <testWorkDir>${project.basedir}</testWorkDir>
                                    <harDir>${project.build.directory}</harDir>
                                    <outputDir>${project.build.directory}/k6/tests</outputDir>
                                </configuration>
                            </execution>

                            <!-- Run k6 load tests -->
                            <execution>
                                <id>run-load-tests</id>
                                <phase>integration-test</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <skip>${k6.skipRun}</skip>
                                    <testDir>${k6.testDir}</testDir>
                                    <virtualUsers>${k6.vus}</virtualUsers>
                                    <duration>${k6.duration}</duration>
                                    <appPort>${app.port}</appPort>
                                    <managementPort>${management.port}</managementPort>
                                    <combineScenarios>${k6.combineScenarios}</combineScenarios>
                                    <scenarioWeights>${k6.scenarioWeights}</scenarioWeights>
                                    <collectVaadinMetrics>${k6.collectVaadinMetrics}</collectVaadinMetrics>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>
</project>
