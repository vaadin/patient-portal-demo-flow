version: '3'
services:
  flow-patient-portal:
    build: .
    environment:
       - spring.datasource.url=jdbc:mysql://$datasourceUrl/pp
    image:
      'repo.vaadin.com:5001/flow-patient-portal:tomcat'
      